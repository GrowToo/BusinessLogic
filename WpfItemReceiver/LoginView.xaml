<Window x:Class="YourNamespace.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <TextBox Text="{Binding Username}" PlaceholderText="Username" Margin="10" />
        <PasswordBox Password="{Binding Password}" Margin="10,50,10,0" />
        <Button Content="Login" Command="{Binding LoginCommand}" Margin="10,100,10,0" />
    </Grid>
</Window>
